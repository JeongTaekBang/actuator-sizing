# Noah Actuator Sizing Tool - Q&A 가이드

## 목차
1. [단위 변환](#1-단위-변환)
2. [Operating Time 선정 로직](#2-operating-time-선정-로직)
3. [DMRA 공식 적용](#3-dmra-공식-적용)
4. [핵심 함수 설명](#4-핵심-함수-설명)
5. [필터링 순서](#5-필터링-순서)
6. [빠진 로직 및 개선점](#6-빠진-로직-및-개선점)
7. [DB 구조 리뷰](#7-db-구조-리뷰)
8. [사이징 프로그램의 본질](#8-사이징-프로그램의-본질)
9. [DB-코드 정합성 검증](#9-db-코드-정합성-검증)

---

## 1. 단위 변환

### Q: 토크를 kgf.m으로 입력하면 DB의 Nm 값과 어떻게 비교하나요?

**A:** `modHelpers.bas`의 `ConvertTorqueToNm()` 함수가 자동 변환합니다.

### 변환 상수

| 변환 | 상수 | 공식 |
|------|------|------|
| lbf.ft → Nm | 1.35582 | `Nm = lbf.ft × 1.35582` |
| kgf.m → Nm | 9.80665 | `Nm = kgf.m × 9.80665` |
| lbf → kN | 0.00444822 | `kN = lbf × 0.00444822` |
| kgf → kN | 0.00980665 | `kN = kgf × 0.00980665` |

### 변환 흐름도

```
┌────────────────────────────────────────────────────────────────┐
│  사용자 입력                                                    │
│  ValveList: Torque = 100 (kgf.m)                               │
│  Settings: TorqueUnit = "kgf.m", SafetyFactor = 1.25           │
└───────────────────────┬────────────────────────────────────────┘
                        │
                        ▼
┌────────────────────────────────────────────────────────────────┐
│  SizeLine() 에서 변환                                           │
│  reqTorque = ConvertTorqueToNm(100, "kgf.m")                   │
│            = 100 × 9.80665 = 980.665 Nm                        │
│  reqTorque = 980.665 × 1.25 = 1225.83 Nm (안전율 적용)          │
└───────────────────────┬────────────────────────────────────────┘
                        │
                        ▼
┌────────────────────────────────────────────────────────────────┐
│  DB 비교 (DB_Actuators는 항상 Nm)                               │
│  액추에이터 Torque_Nm >= 1225.83 인 모델 검색                    │
└────────────────────────────────────────────────────────────────┘
```

---

## 2. Operating Time 선정 로직

### Q: Operating Time을 입력하면 적절한 RPM/Gearbox가 선정되나요?

**A:** 네. 각 액추에이터(+기어박스) 조합의 **계산된 OpTime**이 **요구 범위 내**인 것만 필터링됩니다.

### Operating Time 계산 공식

| 타입 | 직접 구동 | 기어박스 사용 |
|------|----------|--------------|
| **Multi-turn** | `(Turns × 60) / RPM` | `(Turns × Ratio × 60) / RPM` |
| **Part-turn** | `15 / RPM` | `(Ratio × 60) / (4 × RPM)` |

### 허용 범위 검증

```
Settings:
  Op. Time Min (%): -50  →  허용 하한 = 요구시간 × (1 - 0.50)
  Op. Time Max (%): +50  →  허용 상한 = 요구시간 × (1 + 0.50)

예시:
  요구 작동시간: 60초
  Min%: -50, Max%: +50

  허용 범위: 60 × 0.5 ~ 60 × 1.5 = 30초 ~ 90초

       30초        60초        90초
        ├───────────┼───────────┤
        │    허용 범위 (OK)     │
```

### 선정 흐름도

```
┌────────────────────────────────────────────────────────────────────┐
│  사용자 입력                                                        │
│  - 요구 Op.Time: 60초                                               │
│  - Turns: 50회전 (Multi-turn)                                       │
│  - Settings: Op.Time Min: -50%, Max: +50%                           │
│             → 허용 범위: 30초 ~ 90초                                 │
└───────────────────────┬────────────────────────────────────────────┘
                        │
                        ▼
┌────────────────────────────────────────────────────────────────────┐
│  Phase 1: 직접 구동 검사                                            │
│  calcOpTime = (Turns × 60) / RPM = (50 × 60) / 24 = 125초          │
│  125초가 30~90초 범위 밖 → ❌ 제외                                  │
└────────────────────────────────────────────────────────────────────┘
                        │
                        ▼
┌────────────────────────────────────────────────────────────────────┐
│  Phase 2: 기어박스 조합 검사                                        │
│                                                                     │
│  GB-04 (Ratio=4):                                                   │
│    calcOpTime = (50 × 4 × 60) / 24 = 500초 → ❌ 너무 느림           │
│                                                                     │
│  ... 만약 RPM=48인 액추에이터가 있다면:                              │
│    calcOpTime = (50 × 1 × 60) / 48 = 62.5초 → ✅ 30~90초 범위 내!   │
└────────────────────────────────────────────────────────────────────┘
```

### 핵심 포인트

- 기어비(Ratio)가 **높을수록** Operating Time이 **늘어남**
- 느린 작동이 필요하면 → 높은 기어비 기어박스가 선정됨
- 빠른 작동이 필요하면 → 낮은 기어비 or 직접 구동이 선정됨

---

## 3. DMRA 공식 적용

### Q: DMRA "Formulas to help when quoting" 공식이 잘 적용되었나요?

**A:** 네, 모든 공식이 정확하게 구현되어 있습니다.

### 공식 비교표

| 케이스 | DMRA 공식 | 코드 (`CalculateOpTime`) | 상태 |
|--------|----------|--------------------------|:----:|
| Multi-turn (직접) | `(Turns × 60) / RPM` | `(turns * 1 * 60) / rpm` | ✅ |
| Multi-turn (기어박스) | `(Turns × Ratio × 60) / Actuator RPM` | `(turns * gbRatio * 60) / rpm` | ✅ |
| Part-turn (직접) | `60 / (4 × RPM)` | `(1 * 60) / (4 * rpm)` | ✅ |
| Part-turn (기어박스) | `(Ratio × 60) / (4 × RPM)` | `(gbRatio * 60) / (4 * rpm)` | ✅ |

### 출력 토크 공식

| DMRA | 코드 | 비고 |
|------|------|------|
| `Actuator Torque × Mechanical Advantage` | `act.Torque * gb.Ratio * gb.Efficiency` | 효율까지 반영하여 더 정확 |

---

## 4. 핵심 함수 설명

### 4.1 FindBestActuator()

**위치:** `modSizing.bas`  
**역할:** 조건 충족하는 **최저가 1개** 모델 선정

```
┌─────────────────────────────────────────────────────────────┐
│                    FindBestActuator()                        │
├─────────────────────────────────────────────────────────────┤
│  입력:                                                       │
│  - reqTorque: 요구 토크 (Nm, 안전율 적용됨)                   │
│  - reqThrust: 요구 추력 (kN, 안전율 적용됨)                   │
│  - reqOpTime: 요구 작동시간 (초)                             │
│  - reqTurns: 회전수                                          │
│  - reqStemDim: 밸브 스템 직경 (mm)                           │
│  - s: Settings (단위, 전원, Enclosure 등)                    │
├─────────────────────────────────────────────────────────────┤
│  Phase 1: 직접 구동 (액추에이터만)                            │
│           → 조건 충족하는 최저가 모델 찾기                    │
├─────────────────────────────────────────────────────────────┤
│  Phase 2: 기어박스 조합 (FindActuatorWithGearbox 호출)        │
│           → 조건 충족하는 최저가 조합 찾기                    │
├─────────────────────────────────────────────────────────────┤
│  Phase 3: 비교                                               │
│           → 둘 다 성공 시 더 저렴한 쪽 선택                   │
├─────────────────────────────────────────────────────────────┤
│  출력: SizingResult (Model, Gearbox, Torque, OpTime, Price)  │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 FindAllAlternatives()

**위치:** `modMain.bas`  
**역할:** 조건 충족하는 **모든 후보** 반환 (사용자 선택용)

### 4.3 비교표

| | `FindBestActuator()` | `FindAllAlternatives()` |
|---|---|---|
| **위치** | `modSizing.bas` | `modMain.bas` |
| **반환** | **최저가 1개** (`SizingResult`) | **모든 후보** (`Collection`) |
| **용도** | 자동 사이징 | 사용자 수동 선택 |
| **호출** | `btn_SizingAll`, `btn_SizingSelected` | `btn_Alternative` |

### 4.4 PassesActuatorFilters()

**위치:** `modHelpers.bas`  
**역할:** 공통 필터링 조건 체크 (코드 중복 제거용)

```
┌─────────────────────────────────────────────────────────────────┐
│  같은 필터링 로직이 두 군데에서 필요:                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  modSizing.bas                    modMain.bas                   │
│  ┌─────────────────────┐         ┌─────────────────────┐       │
│  │ FindBestActuator()  │         │ FindAllAlternatives()│       │
│  │ (자동 사이징)        │         │ (대체 모델 조회)      │       │
│  └──────────┬──────────┘         └──────────┬──────────┘       │
│             │                               │                   │
│             └───────────┬───────────────────┘                   │
│                         │                                       │
│                         ▼                                       │
│            ┌─────────────────────────────┐                     │
│            │  PassesActuatorFilters()    │ ← 공통 함수         │
│            │  (modHelpers.bas)           │                     │
│            └─────────────────────────────┘                     │
└─────────────────────────────────────────────────────────────────┘
```

### 체크 항목

| 순서 | 조건 | 설명 |
|:----:|------|------|
| 1 | **ActType** | Multi-turn / Part-turn |
| 2 | **Series** | NA / SA / All (ModelRange) |
| 3 | **Voltage** | 110 / 220 / 380 / 440V |
| 4 | **Phase** | 1 / 3 |
| 5 | **Frequency** | 50 / 60 Hz |
| 6 | **Enclosure** | Waterproof (IP67) / Explosionproof (Exd) |
| 7 | **Thrust** | Multi-turn만, 요구추력 이상 |

---

## 5. 필터링 순서

### Q: FindBestActuator는 ActType부터 순차적으로 필터링하나요?

**A:** 네, 순차적으로 필터링해 나갑니다.

### 전체 필터링 순서

```
┌────────────────────────────────────────────────────────────────────┐
│  DB_Actuators 전체 (예: 100개)                                      │
└───────────────────────┬────────────────────────────────────────────┘
                        │
         ①-1. ActType ──┼── Multi-turn / Part-turn 필터
                        │   (예: 60개 → 40개)
                        ▼
         ①-2. Series ───┼── NA / SA / All 필터
                        │   (예: 40개 → 35개)
                        ▼
         ①-3~5. Power ──┼── 380V 3ph 50Hz 필터
                        │   (예: 35개 → 20개)
                        ▼
         ①-6. Enclosure ┼── Waterproof / Explosionproof 필터
                        │   (예: 20개 → 18개)
                        ▼
         ①-7. Thrust ───┼── ≥ 요구추력 (Multi-turn만)
                        │   (예: 18개 → 15개)
                        ▼
         ② Torque ──────┼── ≥ 요구토크
                        │   (예: 15개 → 8개)
                        ▼
         ③ MaxStemDim ──┼── ≥ 밸브 스템 직경
                        │   (예: 8개 → 6개)
                        ▼
         ④ OpTime ──────┼── 허용 범위 내 (±50%)
                        │   (예: 6개 → 3개)
                        ▼
         ⑤ 최저가 선택 ─┼── 3개 중 가장 저렴한 1개!
                        │
                        ▼
                   ✅ 최종 결과
```

### 핵심 포인트

- **순차적 필터링**: 각 단계에서 조건 불충족 시 즉시 `GoTo NextActuator`로 건너뜀
- **조기 탈출**: 비싼 연산(OpTime 계산)은 나중에 → 성능 최적화
- **실패 사유 추적**: 각 단계별 카운트로 실패 사유 정확히 파악

---

## 6. 빠진 로직 및 개선점

### Q: 사이징 로직에서 빠진 게 있나요?

**A:** 몇 가지 추후 개선 가능한 점이 있습니다.

### 현재 상태 요약표

| 항목 | 현재 상태 | 결정 |
|------|:--------:|------|
| ActType 필터 | ✅ 구현됨 | - |
| Power (V/Ph/Hz) | ✅ 구현됨 | - |
| Enclosure | ✅ 구현됨 | - |
| Torque ≥ 요구 | ✅ 구현됨 | - |
| Thrust ≥ 요구 | ✅ 구현됨 | - |
| OpTime 범위 | ✅ 구현됨 | - |
| MaxStemDim | ✅ 구현됨 | - |
| Gearbox OutputTorqueMax | ✅ 구현됨 | - |
| **Operation Mode** | ❌ 미사용 | 추후 발전 |
| **밸브 최대 허용 토크** | ❌ 없음 | 추후 발전 (밸브사 책임) |
| **온도 범위** | ❌ 없음 | 추후 입력 예정 |
| **Weight 출력** | ❌ 미구현 | 추후 구현 |
| **밸브 플랜지 호환** | ❌ 없음 | **불필요** |

### 밸브 플랜지가 불필요한 이유

**ISO 5211 표준**에 따라 플랜지 크기는 토크 범위별로 정해져 있습니다:

| 플랜지 | 토크 범위 (Nm) |
|--------|---------------|
| F05 | ~30 |
| F07 | ~60 |
| F10 | ~125 |
| F14 | ~250 |
| F16 | ~500 |
| F25 | ~1000+ |

**토크를 만족하면 플랜지도 자동으로 맞으므로 별도 검증 불필요!**

### 밸브 최대 허용 토크 관련

현재 접근 방식:
```
┌─────────────────────────────────────────────────────────────────────┐
│  액추에이터 공급사 (우리)                                            │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │  Datasheet 제공:                                              │ │
│  │  - 출력 토크: 900 Nm  ← "이게 우리 출력 토크야"                │ │
│  └───────────────────────────────────────────────────────────────┘ │
└───────────────────────────┬─────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│  밸브 제조사 (상대방)                                                │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │  "900 Nm이면 우리 밸브에 적합한지 우리가 확인할게"              │ │
│  │  → 밸브사 책임으로 호환성 검토                                 │ │
│  └───────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 7. DB 구조 리뷰

### Q: DB 구조(엑셀 컬럼)는 어떤가요?

**A:** **플랫 구조 + 옵션 테이블** 방식으로 설계되어 데이터 관리가 효율적입니다.

### 액추에이터 DB (4개 테이블)

#### DB_Models (18개 컬럼) - 기본 사양 (플랫 구조)
| 구분 | 컬럼 | 용도 |
|------|------|------|
| **기본** | Model, Series, ActType, MotorPower_kW, ControlType, Phase, Freq | 식별/분류 |
| **성능** | RPM, Torque_Nm, Thrust_kN, OpTime_sec, DutyCycle | 사이징 핵심 |
| **연결** | OutputFlange, MaxStemDim_mm | 호환성 |
| **기타** | Weight_kg, BasePrice, Speed_mm_sec, Stroke_mm | 출력/비용/Linear용 |

> **플랫 구조**: 같은 Model이 Freq/kW/RPM 조합별로 여러 행 등록

#### DB_PowerOptions (5개 컬럼) - 전원 옵션
| 구분 | 컬럼 | 용도 |
|------|------|------|
| **키** | Model, Voltage, Phase, Freq | 조합 식별 |
| **가격** | PriceAdder | 가격 추가 |

#### DB_EnclosureOptions (3개 컬럼) - 보호등급
| 구분 | 컬럼 | 용도 |
|------|------|------|
| **키** | Model, Enclosure | 조합 식별 |
| **가격** | PriceAdder | 가격 추가 |

#### DB_ElectricalData (11개 컬럼) - Datasheet용
| 구분 | 컬럼 | 용도 |
|------|------|------|
| **키** | Model, Voltage, Phase, Freq | 조합 식별 |
| **전기** | StartingCurrent_A, StartingPF, RatedCurrent_A, AvgCurrent_A, AvgPF, AvgPower_kW, MotorPoles | Datasheet용 |

### DB_Gearboxes (10개 컬럼)

| 구분 | 컬럼 | 용도 |
|------|------|------|
| **기본** | Model | 식별 |
| **성능** | Ratio, Efficiency | 토크/시간 계산 |
| **제한** | InputTorqueMax, OutputTorqueMax | 필터링 |
| **연결** | InputFlange, OutputFlange, MaxStemDim_mm | 호환성 |
| **기타** | Weight_kg, Price | 출력/비용 |

### 플랫 구조의 장점

| 항목 | 설명 |
|------|------|
| ✅ VBA 코드 단순화 | 복합 키 조회 시 한 테이블에서 직접 필터링 |
| ✅ 카탈로그 일치 | 시리즈별 특성(Freq/kW/RPM별 토크)을 그대로 반영 |
| ✅ 옵션 분리 | 전원/Enclosure 옵션은 별도 테이블로 관리 |
| ✅ 가격 유연성 | BasePrice + PowerAdder + EnclosureAdder |

### 추후 고려할 점

| 항목 | 현재 | 비고 |
|------|:----:|------|
| OperationMode | ❌ | On-Off / Modulating 구분 |
| 온도 범위 | ❌ | AmbientTemp_Min/Max |
| Duty Cycle | ❌ | S2, S4 등 |
| 인증 정보 | ❌ | CE, ATEX, UL 등 |

---

## 8. 사이징 프로그램의 본질

### Q: 직관적으로 사이징 프로그램이 뭔가요?

**A:** 조건에 맞는 모델을 DB에서 필터링해서 최저가 1개를 꺼내오는 것입니다.

### 직관적 흐름

```
┌─────────────────────────────────────────────────────────────────────┐
│  DB_Actuators + DB_Gearboxes                                        │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  📦 📦 📦 📦 📦 📦 📦 📦 📦 📦 📦 📦 📦 📦 📦 📦 📦 📦    │   │
│  │  (전체 모델 + 조합)                                          │   │
│  └─────────────────────────────────────────────────────────────┘   │
└───────────────────────────┬─────────────────────────────────────────┘
                            │
                            ▼ 필터링
┌─────────────────────────────────────────────────────────────────────┐
│  조건: Multi-turn, 380V 3ph 50Hz, IP67, ≥500Nm, OpTime 30~90초      │
└───────────────────────────┬─────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│  남은 후보들                                                         │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  📦 📦 📦  (조건 충족하는 것들만)                             │   │
│  └─────────────────────────────────────────────────────────────┘   │
└───────────────────────────┬─────────────────────────────────────────┘
                            │
                            ▼ 최저가 선택
┌─────────────────────────────────────────────────────────────────────┐
│  결과: 📦 NA-100 + GB-10 = $1,800                                   │
└─────────────────────────────────────────────────────────────────────┘
```

### 한마디로

| 기능 | 설명 |
|------|------|
| **FindBestActuator** | 조건 필터링 → 최저가 **1개** |
| **FindAllAlternatives** | 조건 필터링 → **전부** (사용자 선택용) |

> **계산**은 Operating Time, 출력 토크 정도이고,  
> **본질**은 **"DB 필터링 + 정렬(가격순)"** 입니다!

---

## 9. DB-코드 정합성 검증

### Q: 실제 데이터를 입력할 때 DB 구조와 코드가 맞나요?

**A:** 네, 플랫 구조 DB_Models + 옵션 테이블(4개)과 VBA 코드가 정확히 일치합니다.

### DB_Models 컬럼 매핑 (Excel ↔ VBA)

| 컬럼# | Excel 헤더 | VBA 필드 (ModelRecord) | 타입 | ✅ |
|:-----:|-----------|------------------------|------|:--:|
| 1 | Model | `.Model` | String | ✅ |
| 2 | Series | `.Series` | String | ✅ |
| 3 | Type | `.ActType` | String | ✅ |
| 4 | Torque_Nm | `.Torque` | Double | ✅ |
| 5 | Thrust_kN | `.Thrust` | Double | ✅ |
| 6 | RPM | `.RPM` | Double | ✅ |
| 7 | OutputFlange | `.OutputFlange` | String | ✅ |
| 8 | MaxStemDim_mm | `.MaxStemDim` | Double | ✅ |
| 9 | Weight_kg | `.Weight` | Double | ✅ |
| 10 | BasePrice | `.BasePrice` | Double | ✅ |

### DB_PowerOptions 컬럼 매핑

| 컬럼# | Excel 헤더 | 용도 |
|:-----:|-----------|------|
| 1 | Model | 조인 키 |
| 2 | Voltage | 전압 |
| 3 | Phase | 위상 |
| 4 | Freq | 주파수 |
| 5 | PriceAdder | 가격 추가 |

### DB_EnclosureOptions 컬럼 매핑

| 컬럼# | Excel 헤더 | 용도 |
|:-----:|-----------|------|
| 1 | Model | 조인 키 |
| 2 | Enclosure | 보호등급 |
| 3 | PriceAdder | 가격 추가 |

### DB_Gearboxes 컬럼 매핑 (Excel ↔ VBA)

| 컬럼# | Excel 헤더 | VBA 필드 | 타입 | ✅ |
|:-----:|-----------|----------|------|:--:|
| 1 | Model | `.Model` | String | ✅ |
| 2 | Ratio | `.Ratio` | Double | ✅ |
| 3 | InputTorqueMax | `.InputTorqueMax` | Double | ✅ |
| 4 | OutputTorqueMax | `.OutputTorqueMax` | Double | ✅ |
| 5 | Efficiency | `.Efficiency` | Double | ✅ |
| 6 | InputFlange | `.InputFlange` | String | ✅ |
| 7 | OutputFlange | `.OutputFlange` | String | ✅ |
| 8 | MaxStemDim_mm | `.MaxStemDim` | Double | ✅ |
| 9 | Weight_kg | `.Weight` | Double | ✅ |
| 10 | Price | `.Price` | Double | ✅ |

### 플랫 구조 + 옵션 테이블에서 데이터 조회 흐름

```
┌─────────────────────────────────────────────────────────────────────┐
│  1. ReadModelRecord() - DB_Models에서 기본 사양 읽기                  │
│          ↓                                                          │
│  2. HasPowerOption() - DB_PowerOptions에서 전원 옵션 확인            │
│          ↓                                                          │
│  3. HasEnclosureOption() - DB_EnclosureOptions에서 Enclosure 확인   │
│          ↓                                                          │
│  4. ResolveActuator() - Model + Power + Enclosure 조인              │
│          ↓                                                          │
│  → ActuatorRecord 생성 (Price = BasePrice + PowerAdder + EncAdder)  │
└─────────────────────────────────────────────────────────────────────┘
```

### 코드 구조 (modHelpers.bas)

```
ReadModelRecord()                       ReadGearboxRecord()
┌──────────────────────────┐           ┌──────────────────────────┐
│ Col 1  → .Model          │           │ Col 1  → .Model          │
│ Col 2  → .Series         │           │ Col 2  → .Ratio          │
│ Col 3  → .ActType        │           │ Col 3  → .InputTorqueMax │
│ Col 4  → .Torque         │           │ Col 4  → .OutputTorqueMax│
│ Col 5  → .Thrust         │           │ Col 5  → .Efficiency     │
│ Col 6  → .RPM            │           │ Col 6  → .InputFlange    │
│ Col 7  → .OutputFlange   │           │ Col 7  → .OutputFlange   │
│ Col 8  → .MaxStemDim     │           │ Col 8  → .MaxStemDim     │
│ Col 9  → .Weight         │           │ Col 9  → .Weight         │
│ Col 10 → .BasePrice      │           │ Col 10 → .Price          │
└──────────────────────────┘           └──────────────────────────┘
```

### 주의사항

⚠️ **컬럼 수정 시 동기화 필요!**

| 작업 | 수정 파일 |
|------|----------|
| 컬럼 추가/삭제/이동 | `create_workbook.py` |
| ↓ 동기화 필요 | ↓ |
| VBA 타입/함수 | `modHelpers.bas` (ModelRecord, ReadModelRecord, etc.) |

실제 데이터 입력 시 **컬럼 순서만 유지**하면 로직이 정상 동작합니다!

---

## 문서 버전

- **v2.0** (2025.12) - 플랫 DB 구조 + 옵션 테이블 반영 (DB_Models, DB_PowerOptions, DB_EnclosureOptions, DB_ElectricalData)
- **v1.1** (2025.12) - DB-코드 정합성 검증 섹션 추가
- **v1.0** (2025.12) - 초기 작성

